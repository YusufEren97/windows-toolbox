# Windows PowerShell UTF-8 encoding fix
if ($env:TOOLBOX_UTF8 -ne "1") {
    $env:TOOLBOX_UTF8 = "1"
    $bytes = (New-Object System.Net.WebClient).DownloadData("https://yusuferenseyrek.com.tr/toolbox")
    $script = [System.Text.Encoding]::UTF8.GetString($bytes)
    Invoke-Expression $script
    $env:TOOLBOX_UTF8 = $null
    return
}
$env:TOOLBOX_UTF8 = $null

[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
[Console]::InputEncoding = [System.Text.Encoding]::UTF8

do {
    Clear-Host
    $ascii = @'
  ████████╗ ██████╗  ██████╗ ██╗     ██████╗  ██████╗ ██╗  ██╗
  ╚══██╔══╝██╔═══██╗██╔═══██╗██║     ██╔══██╗██╔═══██╗╚██╗██╔╝
     ██║   ██║   ██║██║   ██║██║     ██████╔╝██║   ██║ ╚███╔╝ 
     ██║   ██║   ██║██║   ██║██║     ██╔══██╗██║   ██║ ██╔██╗ 
     ██║   ╚██████╔╝╚██████╔╝███████╗██████╔╝╚██████╔╝██╔╝ ██╗
     ╚═╝    ╚═════╝  ╚═════╝ ╚══════╝╚═════╝  ╚═════╝ ╚═╝  ╚═╝
'@
    
    Write-Host $ascii -ForegroundColor Yellow
    Write-Host "               [ YusufEren97 tarafından yapıldı ]" -ForegroundColor Gray
    Write-Host "  --------------------------------------------------" -ForegroundColor DarkGray
    Write-Host ""
    
    Write-Host "   [1] Port Killer V1.0" -ForegroundColor White
    Write-Host "   [2] Sistem Temizleyici V1.1" -ForegroundColor White
    Write-Host "   [3] Windows/Office Aktivasyon (MAS)" -ForegroundColor Cyan
    Write-Host "   [4] WinUtil - Sistem Optimizasyon (TR)" -ForegroundColor Magenta
    Write-Host "   [5] Çıkış" -ForegroundColor Red
    Write-Host ""
    
    Write-Host "  --------------------------------------------------" -ForegroundColor DarkGray
    $choice = Read-Host "   Seçiminiz (1-5)"

    switch ($choice) {
        "1" {
            $path = "$env:temp\portkiller.bat"
            $url = "https://yusuferenseyrek.com.tr/portkiller.bat"
            Write-Host "`n   [+] " -NoNewline -ForegroundColor Green
            Write-Host "Port Killer indiriliyor ve başlatılıyor..." -ForegroundColor Gray
            Invoke-WebRequest $url -OutFile $path
            Start-Process $path -Wait -Verb RunAs
            Write-Host "`n   İşlem tamamlandı. Devam etmek için bir tuşa basın..." -ForegroundColor Gray
            $null = $Host.UI.RawUI.ReadKey("NoEcho,IncludeKeyDown")
        }
        "2" {
            $path = "$env:temp\cleaner.bat"
            $url = "https://yusuferenseyrek.com.tr/cleaner.bat"
            Write-Host "`n   [+] " -NoNewline -ForegroundColor Green
            Write-Host "Sistem Temizleyici indiriliyor ve başlatılıyor..." -ForegroundColor Gray
            Invoke-WebRequest $url -OutFile $path
            Start-Process $path -Wait -Verb RunAs
            Write-Host "`n   İşlem tamamlandı. Devam etmek için bir tuşa basın..." -ForegroundColor Gray
            $null = $Host.UI.RawUI.ReadKey("NoEcho,IncludeKeyDown")
        }
        "3" {
            Write-Host "`n   [+] " -NoNewline -ForegroundColor Cyan
             Write-Host "Aktivasyon Scripti (MAS) çalıştırılıyor..." -ForegroundColor Gray
            irm https://get.activated.win | iex
            Write-Host "`n   Menüye dönmek için bir tuşa basın..." -ForegroundColor Gray
            $null = $Host.UI.RawUI.ReadKey("NoEcho,IncludeKeyDown")
        }
        "4" {
            Write-Host "`n   [+] " -NoNewline -ForegroundColor Magenta
            Write-Host "WinUtil (Türkçe) başlatılıyor..." -ForegroundColor Gray
            irm "https://raw.githubusercontent.com/YusufEren97/WinUtil-Turkish-Edition/main/winutil.ps1" | iex
            Write-Host "`n   Menüye dönmek için bir tuşa basın..." -ForegroundColor Gray
            $null = $Host.UI.RawUI.ReadKey("NoEcho,IncludeKeyDown")
        }
        "5" {
            Write-Host "`n   [!] Toolbox sonlandırılıyor..." -ForegroundColor Red
            Start-Sleep -Seconds 1
            break
        }
        Default {
            Write-Host "`n   [!] Hatalı seçim, tekrar deneyin." -ForegroundColor Red
            Start-Sleep -Seconds 1
        }
    }
} while ($choice -ne "5")